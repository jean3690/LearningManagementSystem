<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lms.mapper.WishlistsMapper">

    <resultMap id="BaseResultMap" type="org.lms.entity.Wishlists">
            <id property="id" column="id" />
            <result property="userId" column="user_id" />
            <result property="courseId" column="course_id" />
            <result property="createdAt" column="created_at" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,course_id,created_at
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from wishlists
        where  id = #{id} 
    </select>
    <select id="list" resultType="org.lms.entity.Wishlists">
        select
        <include refid="Base_Column_List"></include>
        from wishlists
        <where>
            id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
        </where>
    </select>
    <select id="findAll" resultType="org.lms.entity.Wishlists">
        select
        <include refid="Base_Column_List"></include>
        from wishlists
    </select>
    <select id="searchByCreatedAt" resultType="org.lms.entity.Wishlists">
        select
        <include refid="Base_Column_List"></include>
        from wishlists
        <where>
            <if test="createdAt!=null">
                created_at &lt;#{createdAt}
            </if>
        </where>
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from wishlists
        where  id = #{id} 
    </delete>
    <delete id="deleteBatch">
        delete from wishlists
        <where>
            id in
            <foreach collection="ids" item="id" open="(" close=")" separator=",">
                #{id}
            </foreach>
        </where>
    </delete>
    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="org.lms.entity.Wishlists" useGeneratedKeys="true">
        insert into wishlists
        ( id,user_id,course_id,created_at)
        values (#{id},#{userId},#{courseId},#{createdAt})
    </insert>
    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="org.lms.entity.Wishlists" useGeneratedKeys="true">
        insert into wishlists
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="id != null">id,</if>
                <if test="userId != null">user_id,</if>
                <if test="courseId != null">course_id,</if>
                <if test="createdAt != null">created_at,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null">#{id},</if>
                <if test="userId != null">#{userId},</if>
                <if test="courseId != null">#{courseId},</if>
                <if test="createdAt != null">#{createdAt},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="org.lms.entity.Wishlists">
        update wishlists
        <set>
                <if test="userId != null">
                    user_id = #{userId},
                </if>
                <if test="courseId != null">
                    course_id = #{courseId},
                </if>
                <if test="createdAt != null">
                    created_at = #{createdAt},
                </if>
        </set>
        where   id = #{id} 
    </update>
    <update id="updateByPrimaryKey" parameterType="org.lms.entity.Wishlists">
        update wishlists
        set 
            user_id =  #{userId},
            course_id =  #{courseId},
            created_at =  #{createdAt}
        where   id = #{id} 
    </update>
</mapper>
