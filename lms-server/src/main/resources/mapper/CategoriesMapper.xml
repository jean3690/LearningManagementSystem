<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lms.mapper.CategoriesMapper">

    <resultMap id="BaseResultMap" type="org.lms.entity.Categories">
            <id property="id" column="id" />
            <result property="name" column="name" />
            <result property="description" column="description" />
            <result property="parentId" column="parent_id" />
            <result property="sortOrder" column="sort_order" />
            <result property="isActive" column="is_active" />
            <result property="createdAt" column="created_at" />
    </resultMap>

    <sql id="Base_Column_List">
        id,name,description,parent_id,sort_order,is_active,
        created_at
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from categories
        where  id = #{id} 
    </select>
    <select id="list" resultType="org.lms.entity.Categories">
        SELECT
        <include refid="Base_Column_List"/>
        from categories
        WHERE id in
        <foreach collection="ids" separator="," item="id" open="(" close=")">
            #{id}
        </foreach>
    </select>
    <select id="selectByPage" resultType="org.lms.entity.Categories">
        SELECT
        <include refid="Base_Column_List"></include>
        from categories
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from categories
        where  id = #{id} 
    </delete>
    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="org.lms.entity.Categories" useGeneratedKeys="true">
        insert into categories
        ( name,description,parent_id,sort_order,is_active,
        created_at)
        values (#{name},#{description},#{parentId},#{sortOrder},#{isActive},
        #{createdAt})
    </insert>
    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="org.lms.entity.Categories" useGeneratedKeys="true">
        insert into categories
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="id != null">id,</if>
                <if test="name != null">name,</if>
                <if test="description != null">description,</if>
                <if test="parentId != null">parent_id,</if>
                <if test="sortOrder != null">sort_order,</if>
                <if test="isActive != null">is_active,</if>
                <if test="createdAt != null">created_at,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null">#{id},</if>
                <if test="name != null">#{name},</if>
                <if test="description != null">#{description},</if>
                <if test="parentId != null">#{parentId},</if>
                <if test="sortOrder != null">#{sortOrder},</if>
                <if test="isActive != null">#{isActive},</if>
                <if test="createdAt != null">#{createdAt},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="org.lms.entity.Categories">
        update categories
        <set>
                <if test="name != null">
                    name = #{name},
                </if>
                <if test="description != null">
                    description = #{description},
                </if>
                <if test="parentId != null">
                    parent_id = #{parentId},
                </if>
                <if test="sortOrder != null">
                    sort_order = #{sortOrder},
                </if>
                <if test="isActive != null">
                    is_active = #{isActive},
                </if>
                <if test="createdAt != null">
                    created_at = #{createdAt},
                </if>
        </set>
        where   id = #{id} 
    </update>
    <update id="updateByPrimaryKey" parameterType="org.lms.entity.Categories">
        update categories
        set 
            name =  #{name},
            description =  #{description},
            parent_id =  #{parentId},
            sort_order =  #{sortOrder},
            is_active =  #{isActive},
            created_at =  #{createdAt}
        where   id = #{id} 
    </update>
</mapper>
