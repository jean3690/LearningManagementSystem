<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lms.mapper.LessonProgressMapper">

    <resultMap id="BaseResultMap" type="org.lms.entity.LessonProgress">
            <id property="id" column="id" />
            <result property="enrollmentId" column="enrollment_id" />
            <result property="lessonId" column="lesson_id" />
            <result property="status" column="status" />
            <result property="progressPercentage" column="progress_percentage" />
            <result property="lastPosition" column="last_position" />
            <result property="startedAt" column="started_at" />
            <result property="completedAt" column="completed_at" />
            <result property="updatedAt" column="updated_at" />
    </resultMap>

    <sql id="Base_Column_List">
        id,enrollment_id,lesson_id,status,progress_percentage,last_position,
        started_at,completed_at,updated_at
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from lesson_progress
        where  id = #{id} 
    </select>
    <select id="findByStaus" resultType="org.lms.entity.LessonProgress">
        select
        <include refid="Base_Column_List"></include>
        from lesson_progress
        <where>
            enrollment_id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
            and status = #{lessonProgressStatus}
        </where>
        ORDER BY
        <foreach collection="ids" item="id" open="( enrollment_id," close=")" separator=",">
            #{id}
        </foreach>
    </select>
    <select id="findAll" resultType="org.lms.entity.LessonProgress">
        select
        <include refid="Base_Column_List"></include>,users.username
        from lesson_progress
        inner join enrollments on enrollments.id = lesson_progress.enrollment_id
        inner join users on users.id = enrollments.user_id
        <where>
            <if test="username!=null and username!=''">
                users.username = #{username}
            </if>
            <if test="enrollmentId != null">
                and enrollment_id = #{enrollmentId}
            </if>
            <if test="lessonId != null">
                and lesson_id = #{lessonId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="progressPercentage != null">
                and progress_percentage = #{progressPercentage}
            </if>
            <if test="lastPosition != null">
                and last_position = #{lastPosition}
            </if>
            <if test="startedAt != null">
                and started_at &lt;= #{startedAt}
            </if>
            <if test="completedAt != null">
                and completed_at &lt;= #{completedAt}
            </if>
            <if test="updatedAt != null">
                and updated_at &lt; #{updatedAt}
            </if>
        </where>
    </select>
    <select id="search" resultType="org.lms.entity.LessonProgress">
        <include refid="Base_Column_List"></include>,users.username
        from lesson_progress
        inner join enrollments on enrollments.id = lesson_progress.enrollment_id
        inner join users on users.id = enrollments.user_id
        <where>
            <if test="username!=null and username!=''">
                users.username = #{username}
            </if>
            <if test="enrollmentId != null">
                and enrollment_id = #{enrollmentId}
            </if>
            <if test="lessonId != null">
                and lesson_id = #{lessonId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="progressPercentage != null">
                and progress_percentage = #{progressPercentage}
            </if>
            <if test="lastPosition != null">
                and last_position = #{lastPosition}
            </if>
            <if test="startedAt != null">
                and started_at &lt;= #{startedAt}
            </if>
            <if test="completedAt != null">
                and completed_at &lt;= #{completedAt}
            </if>
            <if test="updatedAt != null">
                and updated_at &lt; #{updatedAt}
            </if>
        </where>
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from lesson_progress
        where  id = #{id} 
    </delete>
    <delete id="deleteBatch">
        delete from lesson_progress
        <where>
            id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
        </where>
    </delete>
    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="org.lms.entity.LessonProgress" useGeneratedKeys="true">
        insert into lesson_progress
        ( id,enrollment_id,lesson_id,status,progress_percentage,last_position,
        started_at,completed_at,updated_at)
        values (#{id},#{enrollmentId},#{lessonId},#{status},#{progressPercentage},#{lastPosition},
        #{startedAt},#{completedAt},#{updatedAt})
    </insert>
    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="org.lms.entity.LessonProgress" useGeneratedKeys="true">
        insert into lesson_progress
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="id != null">id,</if>
                <if test="enrollmentId != null">enrollment_id,</if>
                <if test="lessonId != null">lesson_id,</if>
                <if test="status != null">status,</if>
                <if test="progressPercentage != null">progress_percentage,</if>
                <if test="lastPosition != null">last_position,</if>
                <if test="startedAt != null">started_at,</if>
                <if test="completedAt != null">completed_at,</if>
                <if test="updatedAt != null">updated_at,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null">#{id},</if>
                <if test="enrollmentId != null">#{enrollmentId},</if>
                <if test="lessonId != null">#{lessonId},</if>
                <if test="status != null">#{status},</if>
                <if test="progressPercentage != null">#{progressPercentage},</if>
                <if test="lastPosition != null">#{lastPosition},</if>
                <if test="startedAt != null">#{startedAt},</if>
                <if test="completedAt != null">#{completedAt},</if>
                <if test="updatedAt != null">#{updatedAt},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="org.lms.entity.LessonProgress">
        update lesson_progress
        <set>
                <if test="enrollmentId != null">
                    enrollment_id = #{enrollmentId},
                </if>
                <if test="lessonId != null">
                    lesson_id = #{lessonId},
                </if>
                <if test="status != null">
                    status = #{status},
                </if>
                <if test="progressPercentage != null">
                    progress_percentage = #{progressPercentage},
                </if>
                <if test="lastPosition != null">
                    last_position = #{lastPosition},
                </if>
                <if test="startedAt != null">
                    started_at = #{startedAt},
                </if>
                <if test="completedAt != null">
                    completed_at = #{completedAt},
                </if>
                <if test="updatedAt != null">
                    updated_at = #{updatedAt},
                </if>
        </set>
        where   id = #{id} 
    </update>
    <update id="updateByPrimaryKey" parameterType="org.lms.entity.LessonProgress">
        update lesson_progress
        set 
            enrollment_id =  #{enrollmentId},
            lesson_id =  #{lessonId},
            status =  #{status},
            progress_percentage =  #{progressPercentage},
            last_position =  #{lastPosition},
            started_at =  #{startedAt},
            completed_at =  #{completedAt},
            updated_at =  #{updatedAt}
        where   id = #{id} 
    </update>
</mapper>
