spring:
  application:
    name: lmsServerApplication
  profiles:
    active: dev
  data:
    redis:
      host: 192.168.138.133
      port: 6379
      database: 0
      password: ningzaichun
      timeout: 10s
      lettuce:
        pool:
          max-wait: -1ms
          min-idle: 0
          max-idle: 8
          max-active: 8
  datasource:
    type: ${datasource.type}
    driver-class-name: ${datasource.driver-class-name}
    url: ${datasource.url}
    username: ${datasource.username}
    password: ${datasource.password}
    # druid 连接池管理
    druid:
      # 初始化时建立物理连接的个数
      initial-size: ${datasource.druid.initial-size}
      # 连接池的最小空闲数量
      min-idle: ${datasource.druid.min-idle}
      # 连接池最大连接数量
      max-active: ${datasource.druid.max-active}
      # 获取连接时最大等待时间，单位毫秒
      max-wait: ${datasource.druid.max-wait}
      # 申请连接的时候检测，如果空闲时间大于timeBetweenEvictionRunsMillis，执行validationQuery检测连接是否有效。
      test-while-idle: true
      # 既作为检测的间隔时间又作为testWhileIdel执行的依据
      time-between-eviction-runs-millis: 60000
      # 销毁线程时检测当前连接的最后活动时间和当前时间差大于该值时，关闭当前连接(配置连接在池中的最小生存时间)
      min-evictable-idle-time-millis: 30000
      # 用来检测数据库连接是否有效的sql 必须是一个查询语句(oracle中为 select 1 from dual)
      validation-query: select 'x'
mybatis:
  mapper-locations: classpath:mapper/*.xml;
  configuration:
    map-underscore-to-camel-case: true
# springdoc-openapi项目配置
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: '后台管理Api'
      paths-to-match: '/**'
      packages-to-scan: org.lms.controller.admin
    - group: '用户Api'
      paths-to-match:
        - '/**'
      packages-to-scan: org.lms.controller.user
        -
# knife4j的增强配置，不需要增强可以不配
knife4j:
  enable: true
  setting:
    language: zh_cn
pagehelper:
  helper-dialect: mysql
  reasonable: true
server:
  port: 8080